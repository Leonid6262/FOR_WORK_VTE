#pragma once

enum class StatusRet 
{
  ERROR = 0,  
  SUCCESS = 1  
};

enum class State 
{ 
  OFF = 0, 
  ON = 1 
};

enum class Mode 
{ 
  FORBIDDEN = 0, 
  ALLOWED = 1 
};

enum class R 
{ 
  NOT_READY = 0, 
  READY = 1 
};